@model IEnumerable<Sake.Models.Lutrija>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdKorisnika)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdUtakmice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdPobjednika)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NagradniBodovi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdKorisnika)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdUtakmice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdPobjednika)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NagradniBodovi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
